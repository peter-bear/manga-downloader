version: '3.8'

services:
  manga-downloader:
    build: .
    ports:
      - "25000:5000"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - DISPLAY=:99
    restart: unless-stopped